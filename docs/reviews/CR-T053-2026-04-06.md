# CR-T053

- 任务: T053 补 sidebar 护栏与观测
- 日期: 2026-04-06
- 结论: 通过

## 变更范围

- 为 sidebar provider 调用增加默认 timeout、单次 retry 和 token budget 护栏
- `/v1/sidebar/ask` 成功和失败响应都带 `trace_id`
- 新增 sidebar runtime state 文件，记录 timeout/retry/token budget 以及最近一次错误和 trace
- `/v1/sidebar/status` 与 `/v1/status` 输出 timeout、retry、token budget、last error、last trace
- 补充 timeout、token budget、recent error 状态面与 ask 契约测试

## 验证记录

1. `cd atlasx && go test ./internal/sidebar`
   - 退出码: 0
   - 摘要: timeout/retry、token budget、runtime state 测试通过。

2. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: ask 响应 `trace_id`、status 最近错误与新增字段契约测试通过。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，护栏与观测改动没有破坏既有 sidebar/tabs/runtime 主链。

## 审查结论

- provider 调用现在有明确护栏：超时、有限重试、token budget，避免无界挂死或提示词无限膨胀。
- `trace_id` 已成为 ask 成功和失败的统一追踪锚点，最近错误也能通过状态面直接观测。
- 观测信息只包含 timeout、budget、trace 和错误文本，没有把真实密钥写入配置或状态文件。
