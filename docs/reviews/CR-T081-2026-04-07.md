# CR-T081

- 日期: 2026-04-07
- 任务: `T081 补齐新窗口打开控制入口`
- 结论: 已完成。`atlasctl tabs open-window` 与 `POST /v1/tabs/open-window` 已落地，CLI 与 HTTP 共用 `tabs.OpenWindow` 主链，通过 browser websocket 调用 `Target.createTarget(newWindow=true)` 创建新窗口中的 page target，不复用现有 tab，也不引入新的窗口状态面。

## 变更事实

- 新增 `tabs.OpenWindow(...)`
- `tabs.Client` 新增 browser websocket 解析与调用能力
- 新增 CLI:
  - `atlasctl tabs open-window <url>`
- 新增 HTTP:
  - `POST /v1/tabs/open-window`
- 新增测试:
  - `atlasx/internal/tabs/client_test.go`
  - `atlasx/cmd/atlasctl/tabs_open_window_test.go`
  - `atlasx/internal/daemon/tab_open_window_test.go`
  - `atlasx/internal/daemon/tab_open_window_contract_test.go`

## 审查结论

- 已确认动作只通过 browser websocket 创建新窗口:
  - 主链调用 `Target.createTarget`，参数显式包含 `newWindow=true`
- 已确认 CLI 与 HTTP 共用同一套 browser websocket 主链:
  - 都调用 `tabs.OpenWindow(...)`
- 已确认失败显式暴露:
  - 危险 scheme 显式拒绝
  - browser websocket 缺失显式返回错误
  - createTarget 返回后若 target 查回失败，显式返回错误

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/tabs ./internal/daemon ./cmd/atlasctl`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
