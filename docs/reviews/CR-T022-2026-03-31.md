# CR-T022

- 任务: T022 初始化侧边栏问答服务骨架
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/internal/sidebar/service.go`
- `atlasx/internal/sidebar/service_test.go`
- `atlasx/internal/settings/store.go`
- `atlasx/internal/daemon/bootstrap.go`
- `atlasx/internal/daemon/http.go`
- `atlasx/internal/daemon/http_test.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含新增的 sidebar service 测试和 daemon sidebar API 测试。

2. `cd atlasx && go run ./cmd/atlasd`
   - 退出码: 手动中断
   - 摘要: 服务正常监听 `http://127.0.0.1:17537`。

3. `curl -s http://127.0.0.1:17537/v1/sidebar/status`
   - 退出码: 0
   - 摘要: 返回 `configured=false`、`ready=false` 和显式原因 `sidebar qa provider is not configured`。

4. `curl -s -o /tmp/atlasx-sidebar-ask.out -w '%{http_code}' -X POST http://127.0.0.1:17537/v1/sidebar/ask -H 'Content-Type: application/json' -d '{"tab_id":"tab-1","question":"summarize this page"}'`
   - 退出码: 0
   - 摘要: 返回 HTTP `503`。

5. `cat /tmp/atlasx-sidebar-ask.out`
   - 退出码: 0
   - 摘要: 返回错误 JSON `sidebar qa provider is not configured`。

## 审查结论

- 侧边栏问答当前只暴露契约和状态，不返回任何伪造答案。
- 默认未配置路径显式返回 `503`，自动化测试额外覆盖了“已配置但后端未实现”返回 `501`。
- `settings.Config` 已预留 `sidebar_provider`、`sidebar_model`、`sidebar_base_url`，为后续真实模型接入留出明确边界。
