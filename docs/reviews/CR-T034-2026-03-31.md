# CR-T034

- 任务: T034 在 atlasd 根状态中统一导出 runtime 版本口径
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `/v1/status` 新增:
  - `runtime_manifest_sha256`
  - `runtime_manifest_binary_path`
- runtime manifest/status 统一读取 `version/channel/sha256/binary_path`
- `doctor` 与 `runtime status` 输出同步补齐 manifest `sha256`

## 验证记录

1. `cd atlasx && go test ./internal/managedruntime ./internal/daemon ./internal/diagnostics`
   - 退出码: 0
   - 摘要: managedruntime 与 daemon 定向测试通过，覆盖 manifest/status/bootstrap/http 状态字段。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，无回归。

3. `HOME=$(mktemp -d /tmp/atlasx-t034-XXXXXX) go run ./cmd/atlasctl runtime stage --bundle-path "/Applications/Google Chrome.app" --version "local-google-chrome"`
   - 退出码: 0
   - 摘要: 在临时 HOME 下完成真实本机 `Google Chrome.app` 的 managed stage。

4. `HOME=<临时HOME> go run ./cmd/atlasd --listen 127.0.0.1:18537`
   - 退出码: 0
   - 摘要: 守护进程成功启动，供 `/v1/status` 在线验证。

5. `curl -s http://127.0.0.1:18537/v1/status`
   - 退出码: 0
   - 摘要: 返回 `runtime_manifest_version=local-google-chrome`、`runtime_manifest_channel=local`、`runtime_manifest_sha256=<非空>`、`runtime_manifest_binary_path=.../Contents/MacOS/Google Chrome`，同时 `runtime_bundle_present=true`、`runtime_binary_executable=true`。

6. `chmod -R u+w <临时HOME> && rm -rf <临时HOME>`
   - 退出码: 0
   - 摘要: 临时 HOME 已清理，未污染默认工作环境。

## 审查结论

- 根状态 `/v1/status` 现在已经能和 managed runtime manifest/status 共享同一套版本字段口径。
- 无 staged runtime 时，新增字符串字段保持空字符串，不会把 manifest 缺失误报成 runtime ready。
