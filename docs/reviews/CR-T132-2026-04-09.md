# CR-T132

- 日期: 2026-04-09
- 任务: `T132 补齐发布证据摘要中的 runtime 与 provider 元数据`
- 结论: 已扩展 `bash scripts/release_evidence.sh`，会从 `atlasd --once` 输出中自动提取 `runtime_manifest_version`、`runtime_manifest_channel` 和 `sidebar_default_provider` 并写入 `SUMMARY.md`，让发布检查单要求的元数据不再依赖手工补录

## 变更范围

- 更新 `atlasx/scripts/release_evidence.sh`
- 更新 `atlasx/scripts/release_evidence_test.go`
- 更新 `docs/reviews/RELEASE-CHECKLIST-2026-04-07.md`
- 更新 `atlasx/docs/RUNBOOK.md`
- 更新 `docs/reviews/CR-ISSUES-CURRENT-STATE-2026-04-07.md`
- 更新 `docs/reviews/CR-STAGE-ALIGNMENT-2026-04-07.md`
- 更新 `tasks.csv`

## 实现事实

- 发布证据脚本继续只以 `atlasd --once` 为单一事实源，不新增第二套 runtime 或 provider 探测逻辑
- `SUMMARY.md` 现在自动汇总：
  - `runtime_manifest_version`
  - `runtime_manifest_channel`
  - `sidebar_default_provider`
- 当这些字段在 `atlasd --once` 输出中不存在或为空时：
  - 统一写为 `none`
  - 不伪造默认版本、channel 或 provider id

## 验证记录

- `cd atlasx && go test -timeout 30s ./scripts`
  - 退出码: 0
- `cd atlasx && bash scripts/release_evidence.sh /tmp/atlasx-release-evidence`
  - 退出码: 0
  - `SUMMARY.md` 含 `runtime_manifest_version`、`runtime_manifest_channel`、`sidebar_default_provider`
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- 发布证据链已经能自动输出 release checklist 所要求的关键元数据
- 元数据缺失时显式记录为 `none`，没有把空配置伪装成有效发布参数
- 实现仍然复用 `atlasd --once` 的现有 JSON 输出，没有引入额外事实源漂移
