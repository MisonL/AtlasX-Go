# CR-T095

- 日期: 2026-04-08
- 任务: `T095 补齐批量按建议组整理到多窗口入口`
- 结论: 已完成。`atlasctl tabs organize-to-windows` 与 `POST /v1/tabs/organize-to-windows` 已落地，CLI 与 HTTP 共用 `tabgroups.ApplyAllToNewWindows` 主链，先复用既有 `tabgroups.Suggest` 获取全部建议组，再按组顺序逐个复用 `organize-group-to-window` 主链，把全部建议组整理到各自新窗口。

## 变更事实

- 新增 `tabgroups.ApplyAllResult`
- 新增 `tabgroups.ApplyAllToNewWindows(...)`
- 新增 CLI:
  - `atlasctl tabs organize-to-windows`
- 新增 HTTP:
  - `POST /v1/tabs/organize-to-windows`
- 新增测试:
  - `atlasx/internal/tabgroups/apply_test.go`
  - `atlasx/cmd/atlasctl/tabs_organize_to_windows_test.go`
  - `atlasx/internal/daemon/tab_organize_to_windows_test.go`
  - `atlasx/internal/daemon/tab_organize_to_windows_contract_test.go`

## 审查结论

- 已确认入口只复用现有两层主链:
  - 先走 `tabgroups.Suggest(...)` 获取全部建议组
  - 再按组顺序逐个走 `ApplyToNewWindow(...)`
- 已确认 CLI 与 HTTP 共用同一套 organize apply-all 主链:
  - 都调用 `tabgroups.ApplyAllToNewWindows(...)`
- 已确认失败显式暴露:
  - 无建议组时显式返回空 `groups`
  - 任一组整理失败时显式返回错误
  - 不伪造“部分成功后整体完成”的结果

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/tabgroups ./internal/daemon ./cmd/atlasctl`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
