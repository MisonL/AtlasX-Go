# CR-T114

- 日期: 2026-04-08
- 任务: `T114 扩展 Agent 单步执行支持 related_tab 激活动作`
- 结论: 已完成 `related_tab` 单步执行链路，`atlasctl` 与 `atlasd` 继续共用 `internal/agentplan.Execute` 主链；显式确认后可激活推荐标签页，`memory_snippet` 等 preview-only 步骤仍显式拒绝

## 变更范围

- 更新 `atlasx/internal/agentplan/execute.go`
- 更新 `atlasx/internal/agentplan/execute_test.go`
- 更新 `atlasx/cmd/atlasctl/tabs_agent_plan.go`
- 更新 `atlasx/cmd/atlasctl/tabs_agent_execute_test.go`
- 更新 `atlasx/cmd/atlasctl/contract_test.go`
- 更新 `atlasx/cmd/atlasctl/tabs_test.go`
- 更新 `atlasx/internal/daemon/tab_agent_execute.go`
- 更新 `atlasx/internal/daemon/tab_agent_execute_test.go`
- 更新 `atlasx/internal/daemon/contract_test.go`
- 更新 `atlasx/internal/daemon/http_test.go`

## 实现事实

- `agentplan.Execute` 新增 `ExecutionActions`，支持通过统一动作执行器运行 `related_tab`
- `related_tab` 执行行为:
  - 需要显式确认
  - 需要有效 `tab_id`
  - 复用既有 `tabs activate` 主链
  - 不写 `memory/events.jsonl`
  - 输出 `activated_tab_id` 与显式 `rollback` 口径
- `sidebar_summarize` 与 `sidebar_ask` 行为保持不变
- `memory_snippet` 仍为 preview-only，不可执行

## 验证记录

- `cd atlasx && go test ./internal/agentplan`
  - 退出码: 0
- `cd atlasx && go test ./cmd/atlasctl -run 'TestTabsAgentExecute|TestTabsAgentPlan|TestTabsAgentExecuteContract|TestTabsAgentExecuteRelatedTabContract|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./internal/daemon -run 'TestTabAgentExecute|TestTabAgentPlan|TestTabAgentExecuteEndpointContract|TestTabAgentExecuteRelatedTabEndpointContract|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- `atlasctl` 与 `atlasd` 仍共用单一 `agent execute` 主链，没有分叉出第二套执行逻辑
- 执行动作与 provider 调用边界清晰：`related_tab` 不依赖 provider，`sidebar_*` 继续走 provider
- 成功执行后会写 sidebar runtime state，且 memory 事件数保持不增加
