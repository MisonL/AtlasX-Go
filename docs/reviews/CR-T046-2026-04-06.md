# CR-T046

- 任务: T046 实现 runtime plan resolve 与 catalog 解析
- 日期: 2026-04-06
- 结论: 通过

## 变更范围

- 新增本地路径与 HTTPS 两种 catalog 解析能力
- 新增 `runtime plan resolve`，把 channel/version/platform 解析成 install plan
- 为 install plan 追加 `bundle_name` 元数据并接入 status/render
- 在 install 执行阶段校验 catalog 解析出的 bundle 名称

## 验证记录

1. `cd atlasx && go test ./internal/managedruntime`
   - 退出码: 0
   - 摘要: 本地 catalog、远程 HTTPS catalog、缺版本与缺 entry 失败路径通过。

2. `cd atlasx && go test ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: `runtime plan resolve` 命令可生成 install plan 并落盘。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，旧的 `plan create|status|clear` 路径未受破坏。

## 审查结论

- catalog 只参与 plan 解析，不直接触发下载，仍符合控制面边界。
- 平台口径固定为 `darwin-amd64` 默认值，保持 Intel x64 macOS 边界不变。
- `plan create` 与 `plan resolve` 语义并存：前者用于手工 plan，后者用于 catalog 驱动自动规划。
