# CR-T023

- 任务: T023 实现自管 Chromium runtime 发现骨架
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/internal/platform/macos/paths.go`
- `atlasx/internal/platform/chrome/runtime.go`
- `atlasx/internal/platform/chrome/chrome_test.go`
- `atlasx/internal/diagnostics/doctor.go`
- `atlasx/internal/daemon/bootstrap.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含 managed runtime 优先级探测测试。

2. `tmp=$(mktemp -d); runtime_dir="$tmp/Library/Application Support/AtlasX/runtime/Chromium.app/Contents/MacOS"; mkdir -p "$runtime_dir"; printf '#!/bin/sh\nexit 0\n' > "$runtime_dir/Chromium"; chmod +x "$runtime_dir/Chromium"; HOME="$tmp" go run ./cmd/atlasctl doctor`
   - 退出码: 0
   - 摘要: `chrome_source=managed_auto`，`chrome_binary` 指向临时 support root 下的 fake managed bundle。

3. `cd atlasx && go run ./cmd/atlasctl doctor`
   - 退出码: 0
   - 摘要: 当前本机仍显示 `chrome_source=system_auto`，未因 managed 探测骨架破坏现有系统 Chrome 路径。

## 审查结论

- managed runtime 发现根路径固定在 `Application Support/AtlasX/runtime`，边界清晰。
- 诊断与状态口径新增 `chrome_source`，可区分 `config`、`managed_auto`、`system_auto`。
- 本轮只完成发现与优先级探测，没有引入下载、安装或伪造“已内置 runtime”状态。
