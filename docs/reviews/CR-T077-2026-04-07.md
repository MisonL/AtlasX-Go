# CR-T077

- 日期: 2026-04-07
- 任务: `T077 补齐上下文推荐入口`
- 结论: 已完成。`atlasctl tabs recommend-context` 与 `GET /v1/tabs/context-recommendations` 已落地，CLI 与 HTTP 共用 `internal/contextrec.ForPage` 推荐逻辑，入口保持只读，不写 memory。

## 变更事实

- 新增 CLI 子命令:
  - `atlasctl tabs recommend-context <target-id>`
- 新增 HTTP 入口:
  - `GET /v1/tabs/context-recommendations?id=<target-id>`
- 新增推荐逻辑包:
  - `atlasx/internal/contextrec`
- 新增测试:
  - `atlasx/cmd/atlasctl/tabs_context_recommend_test.go`
  - `atlasx/internal/contextrec/recommend_test.go`
  - `atlasx/internal/daemon/tab_context_recommendations_test.go`
  - `atlasx/internal/daemon/tab_context_recommendations_contract_test.go`

## 审查结论

- 已确认入口保持只读:
  - CLI 与 HTTP 仅执行 `Capture`、`List`、`memory.FindRelevantSnippets` 与 `contextrec.ForPage`
  - 未调用 `memory.AppendPageCapture`、`memory.AppendQATurn` 或任何写入动作
- 已确认共用一套推荐逻辑:
  - CLI 与 HTTP 都调用 `contextrec.ForPage`
- 已确认失败显式暴露:
  - `Capture` 失败时 CLI 打印 `capture_error`
  - HTTP 返回结构化 `capture_error` 与 `error`
- 已确认空结果显式返回:
  - 无相关标签和 memory 时返回 `returned=0` / `recommendations=[]`

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/contextrec ./internal/daemon ./cmd/atlasctl`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
