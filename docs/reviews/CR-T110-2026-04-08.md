# CR-T110

- 日期: 2026-04-08
- 任务: `T110 补齐企业策略只读状态面`
- 结论: 已完成 `atlasctl policy status` 与 `GET /v1/policy`，CLI 与 HTTP 复用同一套 `internal/policy.View` 主链，只汇总代码中已存在的治理护栏事实，不新增策略写路径或新配置文件

## 变更范围

- 新增 `atlasx/internal/policy/view.go`
- 新增 `atlasx/internal/policy/view_test.go`
- 新增 `atlasx/cmd/atlasctl/policy.go`
- 新增 `atlasx/cmd/atlasctl/policy_test.go`
- 更新 `atlasx/cmd/atlasctl/main.go`
- 更新 `atlasx/cmd/atlasctl/contract_test.go`
- 新增 `atlasx/internal/daemon/policy.go`
- 新增 `atlasx/internal/daemon/policy_test.go`
- 更新 `atlasx/internal/daemon/bootstrap.go`
- 更新 `atlasx/internal/daemon/contract_test.go`

## 实现事实

- `policy.View` 当前统一导出以下治理护栏事实:
  - `config_file`
  - `default_listen_addr`
  - `loopback_only_default`
  - `remote_control_flag`
  - `remote_control_flag_required`
  - `shared_profile_managed`
  - `sidebar_secrets_persisted`
  - `sidebar_default_provider`
  - `sidebar_provider_count`
  - `sidebar_provider_env_keys`
  - `mirror_allowed_roots`
  - `chrome_import_allowed_roots`
- 事实来源:
  - `settings.DefaultListenAddr`
  - `atlasx/cmd/atlasd/main.go` 中的危险开关口径
  - `sourcepaths.DefaultChromeProfilesRoot`
  - `settings.NewStore(...).Bootstrap()` 读取现有 sidebar provider registry
- 明确不做的内容:
  - 不新增企业策略配置文件
  - 不引入策略写入接口
  - 不伪造实时监听状态、真实密钥或 TCC/权限执行态

## 验证记录

- `cd atlasx && go test ./internal/policy`
  - 退出码: 0
- `cd atlasx && go test ./cmd/atlasctl -run 'TestPolicy|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./internal/daemon -run 'TestPolicy'`
  - 退出码: 0
- `cd atlasx && go run ./cmd/atlasctl policy status`
  - 退出码: 0
  - 关键输出:
    - `default_listen_addr=127.0.0.1:17537`
    - `loopback_only_default=true`
    - `remote_control_flag=--allow-remote-control`
    - `shared_profile_managed=false`
    - `sidebar_secrets_persisted=false`
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- CLI 与 HTTP 共用同一套只读 policy 视图主链，没有分叉实现
- provider 只暴露 `api_key_env` 名称和数量，没有落盘真实密钥
- 镜像与导入白名单只复用既有受信根目录口径，没有放宽来源边界
