# CR-T082

- 日期: 2026-04-08
- 任务: `T082 补齐浏览器窗口列表入口`
- 结论: 已完成。`atlasctl tabs windows` 与 `GET /v1/tabs/windows` 已落地，CLI 与 HTTP 共用 `tabs.Windows` 主链，通过 browser websocket 调用 `Browser.getWindowForTarget` 对当前 page targets 进行窗口分组，保持只读，不执行窗口移动或关闭。

## 变更事实

- 新增 `tabs.WindowSummary`
- 新增 `tabs.Windows(...)`
- 新增 CLI:
  - `atlasctl tabs windows`
- 新增 HTTP:
  - `GET /v1/tabs/windows`
- 新增测试:
  - `atlasx/internal/tabs/windows_test.go`
  - `atlasx/cmd/atlasctl/tabs_window_list_test.go`
  - `atlasx/internal/daemon/tab_window_list_test.go`
  - `atlasx/internal/daemon/tab_window_list_contract_test.go`

## 审查结论

- 已确认入口保持只读:
  - 仅按当前 page targets 查询 `windowId/bounds` 并分组，不执行移动、关闭或创建动作
- 已确认 CLI 与 HTTP 共用同一套窗口查询主链:
  - 都调用 `tabs.Windows(...)`
- 已确认失败显式暴露:
  - browser websocket 缺失显式返回错误
  - `Browser.getWindowForTarget` 失败显式返回错误
  - 无 page targets 时显式返回空 `windows`

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/tabs ./internal/daemon ./cmd/atlasctl`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
