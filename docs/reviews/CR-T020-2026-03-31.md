# CR-T020

- 任务: T020 接入浏览器数据打开 HTTP API 并补 downloads open
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/cmd/atlasctl/browserdata.go`
- `atlasx/cmd/atlasctl/main.go`
- `atlasx/internal/browserdata/actions.go`
- `atlasx/internal/browserdata/actions_test.go`
- `atlasx/internal/daemon/bootstrap.go`
- `atlasx/internal/daemon/http.go`
- `atlasx/internal/daemon/http_test.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含新增的 browserdata URL 解析测试和 daemon HTTP 动作测试。

2. `cd atlasx && go run ./cmd/atlasctl downloads open 0`
   - 退出码: 0
   - 摘要: 成功返回 `opened_download_index=0`，并输出新建标签页 `id` 与 `url`。

3. `cd atlasx && go run ./cmd/atlasctl tabs list`
   - 退出码: 0
   - 摘要: 可观测到由 `downloads open 0` 打开的页面级标签。

4. `cd atlasx && go run ./cmd/atlasd`
   - 退出码: 手动中断
   - 摘要: 服务正常监听 `http://127.0.0.1:17537`。

5. `curl -s -X POST http://127.0.0.1:17537/v1/history/open -H 'Content-Type: application/json' -d '{"index":0}'`
   - 退出码: 0
   - 摘要: 返回 `opened_history_index=0` 和新建标签页 `id`。

6. `curl -s -X POST http://127.0.0.1:17537/v1/downloads/open -H 'Content-Type: application/json' -d '{"index":0}'`
   - 退出码: 0
   - 摘要: 返回 `opened_download_index=0` 和新建标签页 `id`。

7. `curl -s -X POST http://127.0.0.1:17537/v1/bookmarks/open -H 'Content-Type: application/json' -d '{"index":0}'`
   - 退出码: 0
   - 摘要: 返回 `opened_bookmark_index=0` 和新建标签页 `id`。

## 审查结论

- 动作 API 全部使用 `POST`，未把变更动作伪装成 `GET`。
- `downloads open` 对空 `tab_url` 显式报错，不存在静默降级。
- 数据读取仍只依赖已落盘的 mirror/import 文件，未引入伪造数据路径。
- CLI 和 HTTP 共用同一组 URL 解析规则，避免行为漂移。
