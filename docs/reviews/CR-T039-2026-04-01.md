# CR-T039

- 任务: T039 为 atlasctl 增加 runtime plan 子命令
- 日期: 2026-04-01
- 结论: 通过

## 变更范围

- 新增 `atlasctl runtime plan create`
- 新增 `atlasctl runtime plan status`
- 新增 `atlasctl runtime plan clear`

## 验证记录

1. `cd atlasx && go test ./internal/managedruntime ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: install plan store 与 CLI 编译链路通过，覆盖 clear 缺失文件和 URL 校验。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，无回归。

3. `HOME=$(mktemp -d /tmp/atlasx-t039-XXXXXX) go run ./cmd/atlasctl runtime plan create --version 123.0.0 --channel stable --url https://example.com/chromium.zip --sha256 deadbeef --archive-path /tmp/chromium.zip --bundle-path /tmp/Chromium.app`
   - 退出码: 0
   - 摘要: 输出 `install_plan_present=true`、`install_plan_phase=planned`，并写入 `install-plan.json`。

4. `HOME=<临时HOME> go run ./cmd/atlasctl runtime plan status`
   - 退出码: 0
   - 摘要: 读回 `install_plan_source_url=https://example.com/chromium.zip`、`install_plan_archive_path=/tmp/chromium.zip`。

5. `HOME=<临时HOME> go run ./cmd/atlasctl runtime plan clear`
   - 退出码: 0
   - 摘要: 输出 `cleared_install_plan=.../runtime/install-plan.json`，仅清理 plan 文件。

## 审查结论

- `runtime plan` 只是计划文件管理，不会伪造下载或 stage 完成。
- `create` 仍然复用 install plan 的 https URL 校验，错误输入会显式失败。
