# CR-T074

- 任务: T074 补齐 memory 检索控制面入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 新增 `atlasctl memory search <question>`，支持 `--tab-id`、`--title`、`--url`、`--limit`
- 新增 `GET /v1/memory/search`，结构化返回 `question/tab_id/title/url/returned/snippets`
- `internal/memory` 的检索输入新增可选 `limit`，继续由 `memory.FindRelevantSnippets` 统一排序与裁剪
- 补充 CLI、daemon 与 memory retrieval 测试，覆盖空态、limit 与契约字段
- 更新 README、阶段对齐与当前状态文档，收口新的 memory 检索入口事实

## 验证记录

1. `cd atlasx && go test ./internal/memory ./internal/daemon ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: retrieval limit、`/v1/memory/search` 契约与错误路径、`atlasctl memory search` 输出契约全部通过。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，memory 检索控制面未破坏既有 memory list、sidebar、tabs、runtime 与 browser data 主链。

## 审查结论

- 本轮补齐了 Browser memories 的最小检索控制面，操作者现在可以直接复用系统内已有的 memory retrieval，而不必只通过 sidebar 间接触发。
- CLI 与 HTTP 均共用 `memory.FindRelevantSnippets`，没有引入第二套排序或筛选逻辑。
- 无 memory 文件时返回空结果，非法 `limit` 或缺失 `question` 时显式失败，符合 Debug-First 约束。
