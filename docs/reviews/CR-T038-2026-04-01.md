# CR-T038

- 任务: T038 持久化 managed runtime install plan 并接入状态面
- 日期: 2026-04-01
- 结论: 通过

## 变更范围

- 新增 `runtime/install-plan.json` 路径
- 新增 install plan 的读写与状态汇总
- `atlasctl runtime status` 与 `GET /v1/runtime/status` 统一输出 install plan 字段

## 验证记录

1. `cd atlasx && go test ./internal/managedruntime ./internal/daemon ./internal/platform/macos`
   - 退出码: 0
   - 摘要: install plan store、runtime status 和 daemon runtime status 定向测试通过。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，无回归。

3. `HOME=$(mktemp -d /tmp/atlasx-t038-cli-XXXXXX) go run ./cmd/atlasctl runtime status`
   - 退出码: 0
   - 摘要: 在临时 HOME 下预写入 `install-plan.json` 后，文本输出包含 `install_plan_present=true`、`install_plan_phase=verifying`、`install_plan_source_url=https://example.com/chromium.zip`、`install_plan_archive_path=/tmp/chromium.zip`。

4. `cd atlasx && go build -o /tmp/atlasd-t038 ./cmd/atlasd`
   - 退出码: 0
   - 摘要: 构建临时守护进程二进制用于在线 HTTP 验证。

5. `HOME=$(mktemp -d /tmp/atlasx-t038-http-XXXXXX) /tmp/atlasd-t038 --listen 127.0.0.1:18540`
   - 退出码: 0
   - 摘要: 守护进程在带 install-plan.json 的临时 HOME 下启动成功。

6. `curl -s http://127.0.0.1:18540/v1/runtime/status`
   - 退出码: 0
   - 摘要: 返回 JSON 含 `install_plan_present=true`、`install_plan_phase=downloaded`、`install_plan_source_url=https://example.com/chromium.zip`、`install_plan_expected_sha256=deadbeef`。

## 审查结论

- install plan 已进入 runtime 状态面，但仍然只是离线计划文件，不会触发真实下载。
- install plan 与 staged manifest 字段保持分离，避免把“已规划下载”误判成“已装入 runtime”。
