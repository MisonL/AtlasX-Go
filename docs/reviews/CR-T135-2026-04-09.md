# CR-T135

- 日期: 2026-04-09
- 任务: `T135 补齐发布证据摘要中的 atlasd readiness 状态`
- 结论: 已扩展 `bash scripts/release_evidence.sh`，会从 `atlasd --once` 输出中自动提取 `atlasd_ready`、`managed_session_live` 和 `sidebar_qa_ready` 并写入 `SUMMARY.md`，同时把 `ready=false` 收敛成 `release_blocker`

## 变更范围

- 更新 `atlasx/scripts/release_evidence.sh`
- 更新 `atlasx/scripts/release_evidence_test.go`
- 更新 `docs/reviews/RELEASE-CHECKLIST-2026-04-07.md`
- 更新 `atlasx/docs/RUNBOOK.md`
- 更新 `docs/reviews/CR-ISSUES-CURRENT-STATE-2026-04-07.md`
- 更新 `docs/reviews/CR-STAGE-ALIGNMENT-2026-04-07.md`
- 更新 `tasks.csv`

## 实现事实

- 发布证据脚本继续只复用 `atlasd --once` 作为 readiness 的单一事实源
- `SUMMARY.md` 现在自动汇总：
  - `atlasd_ready`
  - `managed_session_live`
  - `sidebar_qa_ready`
- `release_blockers` 现在新增规则：
  - 当 `atlasd_ready != true` 时，写入 `atlasd_ready_false`
- 若布尔字段缺失或不可解析：
  - 统一写为 `false_or_unknown`
  - 不伪造为 `true`

## 验证记录

- `cd atlasx && go test -timeout 30s ./scripts`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0
- `cd atlasx && bash scripts/release_evidence.sh /tmp/atlasx-release-evidence`
  - 退出码: 0
  - `SUMMARY.md` 包含 `atlasd_ready`、`managed_session_live`、`sidebar_qa_ready`

## 审查结论

- 发布摘要已经能直接暴露当前控制面 readiness，而不是只给命令退出码
- 若 `atlasd --once` 退出成功但 `ready=false`，当前实现也会把它显式压成 release blocker
- 实现仍然基于 `atlasd --once` 的单一事实源，没有引入新的状态漂移点
