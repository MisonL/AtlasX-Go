# CR-T138

- 日期: 2026-04-10
- 任务: `T138 补齐发布证据摘要中的 import/log/update 状态快照`
- 结论: 已把 `logs` 与 `updates plan` 状态并入 `atlasd --once`，并扩展 `bash scripts/release_evidence.sh` 从同一事实源自动汇总 `chrome_import_present`、`memory_present`、`logs_present`、`logs_file_count`、`updates_plan_present`、`updates_plan_pending`，发布摘要不再需要人工翻 `atlasd-once.log` 或额外调用分散接口

## 变更范围

- 更新 `atlasx/internal/daemon/bootstrap.go`
- 更新 `atlasx/internal/daemon/bootstrap_test.go`
- 更新 `atlasx/internal/daemon/contract_test.go`
- 更新 `atlasx/scripts/release_evidence.sh`
- 更新 `atlasx/scripts/release_evidence_test.go`
- 更新 `docs/reviews/RELEASE-CHECKLIST-2026-04-07.md`
- 更新 `atlasx/docs/RUNBOOK.md`
- 更新 `docs/reviews/CR-STAGE-ALIGNMENT-2026-04-07.md`
- 更新 `docs/reviews/CR-ISSUES-CURRENT-STATE-2026-04-07.md`
- 更新 `tasks.csv`

## 实现事实

- `atlasd --once` 现在直接输出：
  - `logs_present`
  - `logs_file_count`
  - `logs_latest_file`
  - `logs_latest_at`
  - `updates_plan_present`
  - `updates_plan_pending`
  - `updates_plan_in_flight`
  - `updates_plan_phase`
- 发布证据脚本继续只复用 `atlasd --once` 作为快照单一事实源，不额外调用 `/v1/logs`、`/v1/updates` 或其他探测命令
- `SUMMARY.md` 现在自动汇总：
  - `chrome_import_present`
  - `memory_present`
  - `logs_present`
  - `logs_file_count`
  - `updates_plan_present`
  - `updates_plan_pending`
- 缺失字段规则：
  - 布尔字段缺失时写 `false_or_unknown`
  - 整数字段缺失时写 `0`

## 验证记录

- `cd atlasx && go test -timeout 30s ./scripts`
  - 退出码: 0
- `cd atlasx && go test -timeout 30s ./internal/daemon`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0
- `cd atlasx && go run ./cmd/atlasd --once`
  - 退出码: 0
  - 实机输出包含 `chrome_import_present=true`
  - 实机输出包含 `logs_present=false`
  - 实机输出包含 `logs_file_count=0`
  - 实机输出包含 `memory_present=false`
  - 实机输出包含 `updates_plan_present=false`
  - 实机输出包含 `updates_plan_pending=false`
- `cd atlasx && bash scripts/release_evidence.sh /tmp/atlasx-release-evidence`
  - 退出码: 0
  - `SUMMARY.md` 包含 `chrome_import_present=true`
  - `SUMMARY.md` 包含 `memory_present=false`
  - `SUMMARY.md` 包含 `logs_present=false`
  - `SUMMARY.md` 包含 `logs_file_count=0`
  - `SUMMARY.md` 包含 `updates_plan_present=false`
  - `SUMMARY.md` 包含 `updates_plan_pending=false`

## 审查结论

- 新增字段都来自 `atlasd --once` 单一事实源，没有引入第二套 release 侧探测逻辑
- `logs` 与 `updates` 状态已经进入统一控制面快照，发布摘要与运行时状态观测口径保持一致
- 当前真实发布仍被 `UNCOVERED` 与真实环境前置条件阻断，本任务没有伪造 readiness 或把未覆盖项改成通过
