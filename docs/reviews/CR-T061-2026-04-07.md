# CR-T061

- 任务: T061 实现选区提问服务入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 在 `sidebar` 层新增 `SelectionAskRequest`、`BuildSelectionQuestion(...)` 与 `AskSelectionWithMemory(...)`
- 选区提问只接收显式 `selection_text + question`，不伪造浏览器原生选区状态
- `atlasd` 新增 `POST /v1/sidebar/selection/ask`
- 选区提问成功后写入 `qa_turn` memory 事件，事件中的 `question` 明确带 `Selected Text` 与 `User Question`
- README、阶段文档与当前状态文档同步补齐 selection ask 事实

## 验证记录

1. `cd atlasx && go test ./internal/sidebar`
   - 退出码: 0
   - 摘要: `BuildSelectionQuestion` 与 `AskSelectionWithMemory` 的 prompt 注入、显式校验测试通过。

2. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: `/v1/sidebar/selection/ask` 结构化响应、memory 写入、空选区拒绝与契约测试通过。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，selection ask 入口未破坏既有 ask/summarize/runtime/tabs 主链。

## 审查结论

- 当前交付的是蓝图里“选区提问”的最小服务入口，控制输入仅落在 HTTP/sidebar/memory 三处，没有扩散到 tabs capture 契约。
- 选区上下文被显式编码进 provider 请求与 memory 事件，后续排障可直接从 trace 与 events.jsonl 还原真实输入。
- 本轮没有引入浏览器端选区自动探测、多窗口联动或 Agent 编排，仍保持 `Layer B` 的最小闭环实现边界。
