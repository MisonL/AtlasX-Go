# CR-T058

- 任务: T058 补发布与恢复文档
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 新增 `atlasx/docs/RUNBOOK.md`，收口 support root 布局、runtime/provider 配置、恢复步骤、真实 gate 操作和发布检查单
- 更新 `atlasx/README.md`，把运行手册接入当前产品入口
- 更新根 `README.md`，把运行手册和 gate 文档暴露到仓库顶层入口

## 验证记录

1. `cd atlasx && go run ./cmd/atlasd --once`
   - 退出码: 0
   - 摘要: 输出包含 `config_file`、`support_root`、`memory_root`、`runtime_manifest_path`、`sidebar_qa_*` 等字段，运行手册中的路径和状态字段说明与代码事实一致。

2. `cd atlasx && bash scripts/e2e_gate.sh`
   - 退出码: 0
   - 摘要: 运行手册引用的真实 gate 命令可执行；当前环境的 `UNCOVERED` 项显式为 runtime verify/install、tabs capture、browser-data open 和 sidebar ask real smoke。

## 审查结论

- 文档只陈述当前代码已经实现的状态、命令和恢复动作，没有宣称尚未存在的自动发布或自动回滚能力。
- provider 文档只引用 `api_key_env` 和环境变量名，没有把真实密钥写入仓库或示例配置。
- 运行手册与 README、gate 文档已形成单一入口链路，后续发布与恢复可直接按文档执行。
