# CR-T025

- 任务: T025 让 launcher 输出命中的 runtime source
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/internal/launcher/state.go`
- `atlasx/internal/launcher/webapp.go`
- `atlasx/internal/launcher/webapp_test.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含 launcher render/state 的 runtime source 回归测试。

2. `tmp=$(mktemp -d); runtime_dir="$tmp/Library/Application Support/AtlasX/runtime/Chromium.app/Contents/MacOS"; mkdir -p "$runtime_dir"; printf '#!/bin/sh\nexit 0\n' > "$runtime_dir/Chromium"; chmod +x "$runtime_dir/Chromium"; HOME="$tmp" go run ./cmd/atlasctl launch-webapp --dry-run`
   - 退出码: 0
   - 摘要: dry-run 输出 `runtime_source=managed_auto`。

3. `cd atlasx && go run ./cmd/atlasctl launch-webapp --url https://openai.com`
   - 退出码: 0
   - 摘要: 启动输出 `runtime_source=system_auto`。

4. `cd atlasx && go run ./cmd/atlasctl status`
   - 退出码: 0
   - 摘要: 受管 session 状态输出包含 `runtime_source=system_auto`。

5. `cd atlasx && go run ./cmd/atlasctl stop-webapp`
   - 退出码: 0
   - 摘要: 后续状态回到 `session=absent`。

## 审查结论

- 本轮只增强可观测性，没有改变共享 profile 与隔离 profile 的启动语义。
- `runtime_source` 已在 dry-run、真实启动结果和受管 session 状态三处统一暴露，便于后续 managed runtime 调试。
