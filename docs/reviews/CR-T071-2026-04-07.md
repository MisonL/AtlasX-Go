# CR-T071

- 任务: T071 补齐 sidebar ask 的 CLI 入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 新增 `atlasctl sidebar ask <tab-id> <question>`
- 将 sidebar CLI 共享主链拆成 helper，统一配置 bootstrap、tab context 抓取、memory 检索、runtime state 落盘和 memory 事件写入
- 补充 atlasctl 测试，覆盖 ask 成功路径与 provider 失败写 runtime state
- 更新 README、阶段对齐与当前状态文档，收口新的 CLI 能力事实

## 验证记录

1. `cd atlasx && go test ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: `sidebar ask` 成功路径会输出 `answer/provider/model/context_summary/trace_id`，失败路径会把错误落到 sidebar runtime state。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，CLI 问答链路未破坏既有 summarize、tabs、daemon、memory、runtime 主链。

## 审查结论

- 本轮补齐了侧边栏问答在 CLI 控制面上的缺口，本地操作者不再只能通过 HTTP 调用 ask。
- `sidebar ask` 与 `sidebar summarize` 现在共用同一套 CLI helper，避免配置、memory 检索和 runtime state 落盘逻辑分叉。
- 成功路径会写 memory 事件并刷新 runtime state；失败路径也会显式写 runtime state，不存在命令失败但状态面无痕迹的静默降级。
