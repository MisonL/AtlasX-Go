# CR-T060

- 任务: T060 实现页内总结服务入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 在 `sidebar` 层新增 `SummarizeRequest` / `SummarizeResponse` 与 `SummarizeWithMemory(...)`
- 复用现有 provider、prompt、memory 检索与 runtime state 主链，引入固定总结提示词 `PageSummaryQuestion`
- `atlasd` 新增 `POST /v1/sidebar/summarize`
- 总结成功后写入 `qa_turn` memory 事件；失败路径延续 ask 的显式错误语义
- README 与 HTTP 公开面同步补齐 summary 入口

## 验证记录

1. `cd atlasx && go test ./internal/sidebar`
   - 退出码: 0
   - 摘要: `SummarizeWithMemory` 契约与 prompt 注入测试通过。

2. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: `/v1/sidebar/summarize` 结构化响应、memory 写入与契约测试通过。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，summary 入口未破坏既有 tabs/runtime/sidebar 主链。

## 审查结论

- 总结能力没有引入第二套 provider 或 memory 实现，而是严格复用现有 ask 主链。
- `summary` 与 `ask` 共享同一套 trace/runtime state/memory 语义，便于后续统一观测和排障。
- 当前交付的是蓝图里“页内总结”的最小服务入口，不宣称已具备选区提问、多窗口或页面建议等更高层能力。
