# CR-T140

- 日期: 2026-04-10
- 任务: `T140 修正 memory site visibility 参数错误的 HTTP 状态码`
- 结论: 已修正 `POST /v1/memory/controls` 在只传 `site_visibility_enabled`、未传 `site_host` 时的参数校验，当前会显式返回 `400` 与明确错误消息，不再把请求错误误报为 `500`

## 变更范围

- 更新 `atlasx/internal/daemon/memory.go`
- 更新 `atlasx/internal/daemon/memory_controls_test.go`
- 更新 `docs/reviews/CR-T140-2026-04-10.md`
- 更新 `tasks.csv`

## 复核事实

- CodeRabbit 对最近 committed 变更给出 2 条提示
- 有效问题:
  - `atlasx/internal/daemon/memory.go`
  - `site_visibility_enabled` 缺少 `site_host` 时，原先会在 `memory.UpdateControls` 返回参数错误后被 handler 统一映射成 `500`
- 已确认误报:
  - `atlasx/docs/RUNBOOK.md` 中 `go run ./cmd/atlasctl memory set-page-visibility hidden`
  - 该命令本来就是全局页面可见性开关，不缺参数

## 验证记录

- `cd atlasx && go test -timeout 30s ./internal/daemon`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- 本次只修正 handler 层参数校验与错误码映射，没有改变 `memory.UpdateControls`、CLI 行为或既有 site visibility 语义
- 非 site visibility 的 `persist_enabled` 与 `page_visibility_enabled` 请求行为保持不变
