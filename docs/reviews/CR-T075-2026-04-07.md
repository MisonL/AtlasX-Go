# CR-T075

- 任务: T075 补齐页面建议基础入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 新增 `internal/suggestions`，基于当前 page context 和 memory snippets 生成结构化页面建议
- 新增 `atlasctl tabs suggest <tab-id>`，输出页面建议、建议类型、prompt 和 reason
- 新增 `GET /v1/tabs/suggestions?id=<tab-id>`，结构化返回页面建议列表
- 建议链路复用现有 `tabs capture` 和 `memory.FindRelevantSnippets`，不接 provider，不写 memory
- 更新 README、阶段对齐与当前状态文档，收口新的页面建议能力事实

## 验证记录

1. `cd atlasx && go test ./internal/suggestions ./internal/daemon ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: 页面建议生成器、`/v1/tabs/suggestions` 契约与错误路径、`atlasctl tabs suggest` 输出契约全部通过。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，页面建议入口未破坏既有 tabs、memory、sidebar、runtime 与 browser data 主链。

## 审查结论

- 本轮补齐了蓝图里“页面建议”的最小入口，操作者现在可以直接基于当前 tab 获取结构化建议，不需要先调用 provider。
- CLI 与 HTTP 共用同一套 `internal/suggestions` 逻辑，没有引入第二套启发式规则。
- 该入口严格只读：capture 失败时显式返回错误，不写 memory，也不伪造建议结果。
