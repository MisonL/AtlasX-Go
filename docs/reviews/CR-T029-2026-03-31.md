# CR-T029

- 任务: T029 接入 runtime clear HTTP API
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/internal/daemon/bootstrap.go`
- `atlasx/internal/daemon/runtime.go`
- `atlasx/internal/daemon/runtime_test.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含 runtime clear HTTP 测试。

2. `HOME=/tmp/atlasx-runtime-api-home-clear go run ./cmd/atlasd`
   - 退出码: 手动中断
   - 摘要: 服务正常监听 `http://127.0.0.1:17537`。

3. `curl -s -X POST http://127.0.0.1:17537/v1/runtime/stage -H 'Content-Type: application/json' -d '{"bundle_path":"/tmp/atlasx-runtime-api-home-clear/src/Chromium.app","version":"123.0.0","channel":"local"}'`
   - 退出码: 0
   - 摘要: 预置 staged runtime。

4. `curl -s -X POST http://127.0.0.1:17537/v1/runtime/clear`
   - 退出码: 0
   - 摘要: 返回 `cleared_runtime_root=...`。

5. `curl -s http://127.0.0.1:17537/v1/runtime/status`
   - 退出码: 0
   - 摘要: 返回 `manifest_present=false`、`bundle_present=false`、`binary_present=false`。

## 审查结论

- clear 动作为 `POST`，语义清晰。
- clear 仅作用于 `Application Support/AtlasX/runtime`，不会触碰系统 Chrome 或其他目录。
- staged runtime 缺失时仍会返回明确错误，不存在静默成功。
