# CR-T085

- 日期: 2026-04-08
- 任务: `T085 补齐新窗口打开 DevTools 入口`
- 结论: 已完成。`atlasctl tabs open-devtools` 与 `POST /v1/tabs/open-devtools` 已落地，CLI 与 HTTP 共用 `tabs.OpenDevToolsWindow` 主链，先解析指定标签页的 `devtools_frontend_url`，再复用 `open-window` 主链在新窗口中打开该 DevTools。

## 变更事实

- 新增 `tabs.OpenDevToolsWindow(...)`
- 新增 CLI:
  - `atlasctl tabs open-devtools <target-id>`
- 新增 HTTP:
  - `POST /v1/tabs/open-devtools`
- 新增测试:
  - `atlasx/internal/tabs/devtools_window_test.go`
  - `atlasx/cmd/atlasctl/tabs_open_devtools_test.go`
  - `atlasx/internal/daemon/tab_open_devtools_test.go`
  - `atlasx/internal/daemon/tab_open_devtools_contract_test.go`

## 审查结论

- 已确认入口只复用现有两条主链:
  - 先走 `DevTools(targetID)` 解析 `devtools_frontend_url`
  - 再走 `OpenWindow(url)` 在新窗口打开
- 已确认 CLI 与 HTTP 共用同一套 open devtools 主链:
  - 都调用 `tabs.OpenDevToolsWindow(...)`
- 已确认失败显式暴露:
  - 标签页缺失 `devtools_frontend_url` 显式返回错误
  - browser websocket 缺失或新窗口打开失败显式返回错误

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/tabs ./internal/daemon ./cmd/atlasctl`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
