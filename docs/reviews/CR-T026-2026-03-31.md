# CR-T026

- 任务: T026 实现本地 managed Chromium bundle stage 命令
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/cmd/atlasctl/main.go`
- `atlasx/cmd/atlasctl/runtime.go`
- `atlasx/internal/managedruntime/stage.go`
- `atlasx/internal/managedruntime/stage_test.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含 stage 成功路径和无效 bundle 失败路径测试。

2. `tmp=$(mktemp -d); src="$tmp/src/Chromium.app/Contents/MacOS"; mkdir -p "$src"; printf '#!/bin/sh\nexit 0\n' > "$src/Chromium"; chmod +x "$src/Chromium"; printf '<plist></plist>\n' > "$tmp/src/Chromium.app/Contents/Info.plist"; HOME="$tmp" go run ./cmd/atlasctl runtime stage --bundle-path "$tmp/src/Chromium.app" --version 123.0.0`
   - 退出码: 0
   - 摘要: 成功输出 `source_bundle`、`staged_bundle`、`binary`、`manifest`、`version`、`channel`、`sha256`。

3. `HOME="$tmp" go run ./cmd/atlasctl doctor`
   - 退出码: 0
   - 摘要: 输出 `chrome_source=managed_auto`，并显示 staged manifest 状态。

4. `HOME="$tmp" go run ./cmd/atlasctl launch-webapp --dry-run`
   - 退出码: 0
   - 摘要: 输出 `binary=<support root/runtime/.../Chromium>` 和 `runtime_source=managed_auto`。

## 审查结论

- 本轮只支持显式本地 bundle path，不涉及下载、升级或自动发现源目录。
- stage 前会校验 `Contents/MacOS/Chromium` 是否存在且可执行，不符合时显式失败。
- staged bundle 固定落到 `Application Support/AtlasX/runtime/Chromium.app`，后续检测链路与 manifest 口径已自动对齐。
