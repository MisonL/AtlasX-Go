# CR-T108

- 日期: 2026-04-08
- 任务: `T108 补齐结构化 doctor 诊断入口`
- 结论: 已完成。`atlasctl doctor --json` 与 `GET /v1/doctor` 已落地，CLI 与 HTTP 共用 `diagnostics.Generate()` 主链返回结构化诊断状态；默认 `atlasctl doctor` 的文本输出保持兼容，没有引入写操作。

## 变更事实

- 为 `diagnostics.Report` 新增 `RenderJSON()`
- 新增 CLI:
  - `atlasctl doctor --json`
- 新增 HTTP:
  - `GET /v1/doctor`
- 新增测试:
  - `atlasx/cmd/atlasctl/doctor_test.go`
  - `atlasx/internal/daemon/doctor_test.go`
  - `atlasx/internal/daemon/doctor_contract_test.go`

## 审查结论

- 已确认 CLI 与 HTTP 共用同一套诊断主链:
  - 都调用 `diagnostics.Generate()`
- 已确认默认文本 doctor 输出未回归:
  - `atlasctl doctor` 仍输出原有文本口径
  - 只有显式 `--json` 才切换为结构化 JSON
- 已确认失败显式暴露:
  - 诊断生成失败时 CLI 直接返回错误
  - `/v1/doctor` 诊断失败时显式返回错误 JSON
- 已确认没有引入第二套诊断事实:
  - JSON 只改变渲染方式，不改变诊断采集逻辑

## 验证记录

- 命令:
  - `cd atlasx && go test ./cmd/atlasctl -run 'TestDoctor|Test.*Contract'`
  - `cd atlasx && go test ./internal/daemon -run 'TestDoctor'`
  - `cd atlasx && go run ./cmd/atlasctl doctor --json`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
  - 本机 CLI 实测输出包含:
    - `"ChromeStatus": "ok"`
    - `"RuntimeManifest"`
    - `"Session"`
