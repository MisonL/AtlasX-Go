# CR-T146

- 日期: 2026-04-10
- 任务: `T146 检查并收敛 sidebar ask real smoke 的真实 provider readiness`
- 结论: 当前开发机的 `sidebar ask real smoke` 阻断不是代码故障，而是缺少真实 provider registry 和对应环境变量；`sidebar_provider_count=0`、`configured=false`、`ready=false` 已被精确定位，当前唯一剩余前置条件就是按 `RUNBOOK` 写入 `sidebar_default_provider + sidebar_providers[]` 并导出对应 `api_key_env`

## 变更范围

- 更新 `docs/reviews/CR-ISSUES-CURRENT-STATE-2026-04-07.md`
- 新增 `docs/reviews/CR-T146-2026-04-10.md`
- 更新 `tasks.csv`

## 真实执行记录

- `cd atlasx && go test ./...`
  - 退出码: 0
- `cd atlasx && go run ./cmd/atlasctl sidebar status`
  - 退出码: 0
  - `configured=false`
  - `ready=false`
  - `provider_count=0`
  - `reason=sidebar qa provider is not configured`
- `cd atlasx && go run ./cmd/atlasctl settings | rg '^sidebar_'`
  - 退出码: 0
  - `sidebar_provider=`
  - `sidebar_model=`
  - `sidebar_base_url=`
  - `sidebar_default_provider=`
  - `sidebar_provider_count=0`
- `cat '/Users/mison/Library/Application Support/AtlasX/config.json'`
  - 退出码: 0
  - 当前仅包含 `chrome_binary`、`default_profile`、`listen_addr`、`web_app_url`
  - 不包含 `sidebar_default_provider` 与 `sidebar_providers`
- `env | rg '^(OPENAI|ANTHROPIC|DASHSCOPE|MOONSHOT|DEEPSEEK|GROQ|ARK|ATLASX|SIDEBAR)_'`
  - 退出码: 1
  - 当前 shell 中没有可供 sidebar 读取的 provider 密钥环境变量
- `cd atlasx && ATLASX_E2E_ALLOW_INSTALL=1 bash scripts/release_evidence.sh /tmp/atlasx-release-evidence-t145`
  - 退出码: 0
  - `uncovered_count=1`
  - 唯一 `UNCOVERED`: `sidebar ask real smoke: sidebar_qa_ready=false，缺失真实 provider 凭据或配置`

## 审查结论

- 当前剩余阻断已经被收缩为精确缺失项，不是代码逻辑失败
- 结合 `atlasx/docs/RUNBOOK.md`，要让 `sidebar_qa_ready=true`，当前机器至少还缺:
  - `config.json` 中的 `sidebar_default_provider`
  - `config.json` 中的 `sidebar_providers[].id/provider/model/base_url/api_key_env`
  - 当前 shell 中与 `api_key_env` 对应的真实环境变量
- 在未提供真实凭据前，不能把 `sidebar ask real smoke` 改写成通过
