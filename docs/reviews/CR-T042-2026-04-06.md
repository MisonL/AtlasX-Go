# CR-T042

- 任务: T042 为公开 CLI 与 HTTP 契约补 golden 测试基线
- 日期: 2026-04-06
- 结论: 通过

## 变更范围

- 为 `atlasctl runtime status|plan status|verify` 新增 CLI 文本契约测试
- 为 `atlasd` 的 status/runtime/sidebar/tabs/history/verify 新增 JSON 字段契约测试
- 断言策略采用关键字段存在与少量关键值校验，不冻结内部实现顺序或完整输出快照

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: `atlasctl` 与 `internal/daemon` 契约测试通过，全仓 Go 测试通过。

2. 契约审查
   - 结论:
     - CLI 已冻结 `runtime status` 的 runtime/manifest/install plan/binary 关键字段
     - CLI 已冻结 `runtime plan status` 的 `source_url` 与 `phase` 关键输出
     - CLI 已冻结 `runtime verify` 失败时仍输出 `manifest_present` 与 `verified`
     - HTTP 已冻结 `/v1/status` `/v1/runtime/status` `/v1/runtime/plan` `/v1/sidebar/status` `/v1/tabs/context` `/v1/history` `/v1/runtime/verify` 的关键 JSON 字段

## 审查结论

- 当前公开 CLI 与 HTTP 用户面已有追加兼容的契约基线，后续新增字段不会破坏现有测试。
- 本任务未将内部结构、完整文本顺序或完整 JSON 快照硬编码为脆弱断言，保持了后续迭代空间。
