# CR-T063

- 任务: T063 实现按标签页解析 DevTools frontend URL
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 在 `tabs` 层新增 `DevTools(...)`，从现有 CDP target 列表解析每个 page target 的 `devtoolsFrontendUrl`
- 支持将相对 `devtoolsFrontendUrl` 解析为基于当前 CDP base URL 的绝对地址
- `atlasd` 新增 `GET /v1/tabs/devtools?id=<target-id>`，按 tab 返回结构化 `devtools_frontend_url`
- 缺失 frontend URL 时显式失败，不伪造调试会话或默认 URL
- README 与阶段事实文档同步补齐最小 DevTools 入口

## 验证记录

1. `cd atlasx && go test ./internal/tabs`
   - 退出码: 0
   - 摘要: 相对 DevTools URL 解析与缺失 frontend URL 失败测试通过。

2. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: `/v1/tabs/devtools` 成功响应、失败透传与契约测试通过。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，未破坏既有 tabs list/context/selection 主链。

## 审查结论

- 本轮交付的是蓝图中 DevTools 的最小服务入口，作用是把“已探测到 CDP/DevTools”推进为“可按 tab 解析可消费 URL”。
- 当前能力只覆盖 frontend URL 暴露，不宣称已具备完整内置 DevTools 面板、设备模拟或多窗口调试壳层。
- 解析逻辑完全复用现有 target 列表事实源，没有额外引入第二套 target 状态真相。
