# CR-T047

- 任务: T047 补受管 session 新鲜度与自愈治理
- 日期: 2026-04-06
- 结论: 通过

## 变更范围

- 为 launcher session 状态面追加 `ready`、`stale`、`state_cleaned`
- 对无进程的陈旧 managed session file 自动清理
- 对进程仍在但 CDP 短暂失联场景加入有限重试自愈
- 让 `/v1/status` 的 `managed_session_live` 改为以 session ready 为准，避免误报

## 验证记录

1. `cd atlasx && go test ./internal/launcher`
   - 退出码: 0
   - 摘要: stale file 清理、CDP 下线 stale 标记、CDP retry 自愈测试通过。

2. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: `/v1/status` 对陈旧 managed session 不再误报 live，且 stale cleanup 状态可观测。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，tabs 与 launcher 现有主链未被破坏。

## 审查结论

- 共享 profile 仍不会被误判为 managed session。
- 无进程 stale session 会自动清理 state file；有进程但 CDP 异常的会话不会被静默清理，而是显式标记 stale，清理边界清晰。
- `/v1/status` 现在以 session ready 而非仅有进程存在作为 live 判定依据，避免“会话已死但状态面仍显示存活”。
