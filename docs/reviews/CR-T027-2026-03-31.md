# CR-T027

- 任务: T027 接入 managed runtime 状态与清理命令
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/cmd/atlasctl/runtime.go`
- `atlasx/internal/managedruntime/status.go`
- `atlasx/internal/managedruntime/status_test.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含 runtime status/clear 成功路径和缺失 runtime 错误路径测试。

2. `tmp=$(mktemp -d); src="$tmp/src/Chromium.app/Contents/MacOS"; mkdir -p "$src"; printf '#!/bin/sh\nexit 0\n' > "$src/Chromium"; chmod +x "$src/Chromium"; printf '<plist></plist>\n' > "$tmp/src/Chromium.app/Contents/Info.plist"; HOME="$tmp" go run ./cmd/atlasctl runtime stage --bundle-path "$tmp/src/Chromium.app" --version 123.0.0`
   - 退出码: 0
   - 摘要: 预置 staged runtime。

3. `HOME="$tmp" go run ./cmd/atlasctl runtime status`
   - 退出码: 0
   - 摘要: 输出 `manifest_present=true`、`bundle_present=true`、`binary_present=true`、`binary_executable=true`。

4. `HOME="$tmp" go run ./cmd/atlasctl runtime clear`
   - 退出码: 0
   - 摘要: 输出 `cleared_runtime_root=...`。

5. `HOME="$tmp" go run ./cmd/atlasctl doctor`
   - 退出码: 0
   - 摘要: 输出 `chrome_source=system_auto` 且 `managed_runtime_manifest_present=false`，确认已退出 staged runtime 路径。

## 审查结论

- `runtime clear` 仅作用于 `Application Support/AtlasX/runtime`，不会触碰系统 Chrome 或用户其他目录。
- staged runtime 缺失时会显式返回 `managed runtime is not staged`，不存在静默成功。
