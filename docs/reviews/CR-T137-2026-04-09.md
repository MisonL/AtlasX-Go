# CR-T137

- 日期: 2026-04-09
- 任务: `T137 补齐发布证据摘要中的关键环境快照`
- 结论: 已扩展 `bash scripts/release_evidence.sh`，会从 `atlasd --once` 输出中自动提取 `chrome_source`、`runtime_manifest_present` 和 `mirror_present` 并写入 `SUMMARY.md`，让发布摘要直接暴露当前 runtime 与浏览器数据基线

## 变更范围

- 更新 `atlasx/scripts/release_evidence.sh`
- 更新 `atlasx/scripts/release_evidence_test.go`
- 更新 `docs/reviews/RELEASE-CHECKLIST-2026-04-07.md`
- 更新 `atlasx/docs/RUNBOOK.md`
- 更新 `docs/reviews/CR-ISSUES-CURRENT-STATE-2026-04-07.md`
- 更新 `docs/reviews/CR-STAGE-ALIGNMENT-2026-04-07.md`
- 更新 `tasks.csv`

## 实现事实

- 发布证据脚本继续只复用 `atlasd --once` 作为环境快照的单一事实源
- `SUMMARY.md` 现在自动汇总：
  - `chrome_source`
  - `runtime_manifest_present`
  - `mirror_present`
- 字段缺失规则：
  - `chrome_source` 缺失时写 `none`
  - 布尔字段缺失时写 `false_or_unknown`

## 验证记录

- `cd atlasx && go test -timeout 30s ./scripts`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0
- `cd atlasx && bash scripts/release_evidence.sh /tmp/atlasx-release-evidence`
  - 退出码: 0
  - `SUMMARY.md` 包含 `chrome_source/system_auto`
  - `SUMMARY.md` 包含 `runtime_manifest_present=false`
  - `SUMMARY.md` 包含 `mirror_present=true`

## 审查结论

- 发布摘要已经可以一眼判断当前 runtime 来源、managed runtime 是否已 staged、browser-data 镜像是否已存在
- 实现没有引入第二套环境探测逻辑，仍然复用 `atlasd --once` 单一事实源
- 缺失字段时显式落成 `none` 或 `false_or_unknown`，不会伪造环境已就绪
