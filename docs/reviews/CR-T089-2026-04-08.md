# CR-T089

- 日期: 2026-04-08
- 任务: `T089 补齐标签页搜索入口`
- 结论: 已完成。`atlasctl tabs search` 与 `GET /v1/tabs/search` 已落地，CLI 与 HTTP 共用 `tabs.Search` / `tabs.SearchTargets` 主链，基于当前 page target 列表按 `title/url` 做大小写不敏感匹配，返回结构化命中结果。

## 变更事实

- 新增 `tabs.Search(...)`
- 新增 `tabs.SearchTargets(...)`
- 新增 CLI:
  - `atlasctl tabs search <query>`
- 新增 HTTP:
  - `GET /v1/tabs/search?q=<query>`
- 新增测试:
  - `atlasx/internal/tabs/search_test.go`
  - `atlasx/cmd/atlasctl/tabs_search_test.go`
  - `atlasx/internal/daemon/tab_search_test.go`
  - `atlasx/internal/daemon/tab_search_contract_test.go`

## 审查结论

- 已确认入口只复用现有两条主链:
  - 先走 `List()` 读取当前 page targets
  - 再走 `SearchTargets(...)` 做只读匹配
- 已确认 CLI 与 HTTP 共用同一套搜索匹配逻辑:
  - 都调用 `tabs.Search(...)`
- 已确认失败与边界显式暴露:
  - 缺失 query 时显式返回错误
  - `service_worker` 等非 `page target` 不进入结果

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/tabs ./internal/daemon ./cmd/atlasctl`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
