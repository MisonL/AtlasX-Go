# CR-T115

- 日期: 2026-04-08
- 任务: `T115 扩展 Agent 单步执行支持 memory_snippet 问答动作`
- 结论: 已完成 `memory_snippet` 单步执行链路，`atlasctl` 与 `atlasd` 继续共用 `internal/agentplan.Execute` 主链；显式确认后可基于 snippet 执行单步问答，未支持步骤类型仍显式拒绝

## 变更范围

- 更新 `atlasx/internal/agentplan/execute.go`
- 更新 `atlasx/internal/agentplan/execute_test.go`
- 更新 `atlasx/cmd/atlasctl/tabs_agent_execute_test.go`
- 更新 `atlasx/cmd/atlasctl/contract_test.go`
- 更新 `atlasx/internal/daemon/tab_agent_execute_test.go`
- 更新 `atlasx/internal/daemon/contract_test.go`

## 实现事实

- `agentplan.Execute` 新增 `memory_snippet` 执行分支：
  - 需要显式确认
  - 需要非空 snippet
  - 复用既有 `sidebar.AskWithMemory` 主链执行问答
  - 继续不写 `memory/events.jsonl`
- 新增错误边界：
  - `ErrStepSnippetRequired`（memory snippet 缺失）
- 已有分支保持：
  - `sidebar_summarize` / `sidebar_ask` 保持原行为
  - `related_tab` 保持单步激活行为
  - 未支持步骤类型仍返回 `ErrStepNotExecutable`

## 验证记录

- `cd atlasx && go test ./internal/agentplan`
  - 退出码: 0
- `cd atlasx && go test ./cmd/atlasctl -run 'TestTabsAgentExecute|TestTabsAgentPlan|TestTabsAgentExecuteContract|TestTabsAgentExecuteRelatedTabContract|TestTabsAgentExecuteMemorySnippetContract|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./internal/daemon -run 'TestTabAgentExecute|TestTabAgentPlan|TestTabAgentExecuteEndpointContract|TestTabAgentExecuteRelatedTabEndpointContract|TestTabAgentExecuteMemorySnippetEndpointContract|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- CLI 与 HTTP 继续共用单一 agent execute 主链，没有分叉执行逻辑
- `memory_snippet` 执行明确复用 sidebar provider，不引入第二套模型调用
- memory 事件数量在执行前后保持不增加，符合“不写 memory”边界
