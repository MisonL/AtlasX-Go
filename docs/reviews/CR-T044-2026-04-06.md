# CR-T044

- 任务: T044 暴露 runtime install CLI 与 HTTP 控制面
- 日期: 2026-04-06
- 结论: 通过

## 变更范围

- 为 `managedruntime.Install(...)` 增加并发执行门禁，重复 install 明确返回冲突错误
- 为 `atlasd` 新增 `POST /v1/runtime/install`
- 补 daemon install 成功、缺 plan、并发冲突、方法错误测试
- 同步 README 的 runtime install HTTP 事实

## 验证记录

1. `cd atlasx && go test ./internal/managedruntime`
   - 退出码: 0
   - 摘要: install 并发门禁测试通过。

2. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: `/v1/runtime/install` 的成功与冲突口径通过。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，CLI 与 HTTP 入口都复用同一套 install 状态机。

## 审查结论

- `atlasctl runtime install` 与 `POST /v1/runtime/install` 已统一复用 `managedruntime.Install(...)`，没有引入第二套状态真相。
- install 只能通过 `POST` 触发；缺失 plan、非法 phase 与并发 install 都返回显式冲突错误。
- 当前 install 控制面已闭环到 plan phase/error，但失败后的 archive/临时残留治理仍留在 `T045` 收口。
