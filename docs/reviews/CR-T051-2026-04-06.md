# CR-T051

- 任务: T051 实现 sidebar 第一个真实 provider adapter
- 日期: 2026-04-06
- 结论: 通过

## 变更范围

- 为 sidebar 增加首个 `openai` / `openai-compatible` `/chat/completions` 适配器
- 让 `sidebar.Status()` 基于 provider 类型、`api_key_env` 和环境变量值判断 `ready`
- 让 `/v1/sidebar/ask` 在成功路径下真实抓取当前 tab context，并返回 `answer/provider/model/context_summary`
- provider 未配置继续返回 `503`，不支持的 provider 继续返回 `501`，上游 provider 失败显式返回 `502`
- 补充 sidebar 与 daemon 测试，覆盖 fake server 成功、上游失败、HTTP 契约和上下文真实注入

## 验证记录

1. `cd atlasx && go test ./internal/sidebar`
   - 退出码: 0
   - 摘要: OpenAI-compatible adapter、上下文注入和 provider failure 测试通过。

2. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: `/v1/sidebar/ask` 成功响应、501/503/502 错误码和字段契约测试通过。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，真实 provider adapter 没有破坏 tabs/runtime/import 等既有链路。

## 审查结论

- `sidebar ask` 不再伪造成功，只有在 provider ready 且真实请求成功时才返回结构化答案。
- 当前 tab context 由 daemon 在 ask 前真实抓取，并注入 provider 请求体，满足问答链路的数据面闭环。
- provider 失败口径清晰分层：未配置 `503`、未实现 `501`、上游失败 `502`，没有静默降级。
