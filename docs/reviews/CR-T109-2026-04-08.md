# CR-T109

- 日期: 2026-04-08
- 任务: `T109 补齐 profile 状态只读入口`
- 结论: 已完成。`atlasctl profile status` 与 `GET /v1/profile` 已落地，CLI 与 HTTP 共用 `profile.LoadView()` 主链，汇总默认 profile、选中模式和 isolated profile 目录事实，不引入新的 profile 写操作或迁移逻辑。

## 变更事实

- 新增 `profile.LoadView()`
- 新增 CLI:
  - `atlasctl profile status`
- 新增 HTTP:
  - `GET /v1/profile`
- 新增测试:
  - `atlasx/internal/profile/view_test.go`
  - `atlasx/cmd/atlasctl/profile_test.go`
  - `atlasx/internal/daemon/profile_test.go`
  - `atlasx/internal/daemon/contract_test.go` 中新增 `/v1/profile` 契约断言

## 审查结论

- 已确认 CLI 与 HTTP 共用同一套 profile 视图主链:
  - 都调用 `profile.LoadView()`
- 已确认只做状态汇总:
  - 只读取 settings 默认 profile
  - 只复用 `profile.Store.Resolve()` 解析 selected/isolated 路径
  - 未新增 profile 迁移、切换写入或共享 profile 写路径
- 已确认 shared profile 语义明确:
  - `shared_managed=false`
  - 不伪造共享 profile 目录路径
- 已确认失败显式暴露:
  - 配置 bootstrap 失败显式返回错误
  - profile resolve 失败显式返回错误
  - isolated 目录状态读取异常显式返回错误

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/profile`
  - `cd atlasx && go test ./cmd/atlasctl -run 'TestProfile|Test.*Contract'`
  - `cd atlasx && go test ./internal/daemon -run 'TestProfile'`
  - `cd atlasx && go run ./cmd/atlasctl profile status`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
  - 本机 CLI 实测输出:
    - `default_profile=isolated`
    - `selected_mode=isolated`
    - `isolated_present=true`
    - `shared_managed=false`
