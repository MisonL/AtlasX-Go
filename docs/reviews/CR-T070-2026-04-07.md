# CR-T070

- 任务: T070 补齐 sidebar summarize 的 CLI 入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 新增 `atlasctl sidebar summarize <tab-id>`，可在 CLI 中对指定 tab 执行页内总结
- 复用现有 `sidebar.SummarizeWithMemory`、`memory.FindRelevantSnippets`、`memory.AppendQATurn` 与 `sidebar.SaveRuntimeResult`
- 补充 `atlasctl` 测试，覆盖成功总结与 provider 失败写 runtime state
- 更新 README、阶段对齐与当前状态文档，收口新的 CLI 能力事实

## 验证记录

1. `cd atlasx && go test ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: `sidebar summarize` 成功路径会输出 `summary/provider/model/context_summary/trace_id`，失败路径会把错误落到 sidebar runtime state。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，CLI 总结链路未破坏既有 tabs、daemon、sidebar、memory、runtime 主链。

## 审查结论

- 本轮补齐了页内总结在 CLI 控制面上的缺口，本地操作者不再只能依赖 HTTP 触发 summarize。
- CLI 继续共用 HTTP 已验证的 summarize 主链，没有重新实现第二套 provider/memory/runtime 语义。
- 成功路径会写 memory 事件并刷新 sidebar runtime state；失败路径也会显式写 runtime state，不存在“命令失败但状态面无痕迹”的静默降级。
