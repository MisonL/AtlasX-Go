# CR-T113

- 日期: 2026-04-08
- 任务: `T113 补齐 Agent 单步显式执行入口`
- 结论: 已完成 `atlasctl tabs agent-execute` 与 `POST /v1/tabs/agent-execute`，CLI 与 HTTP 共用同一套 `internal/agentplan.Execute` 主链，要求显式确认后执行单个 agent plan step；当前仅支持 `sidebar_summarize` 与 `sidebar_ask` 类型步骤，preview-only step 会显式拒绝

## 变更范围

- 新增 `atlasx/internal/agentplan/execute.go`
- 新增 `atlasx/internal/agentplan/execute_test.go`
- 更新 `atlasx/cmd/atlasctl/tabs_agent_plan.go`
- 新增 `atlasx/cmd/atlasctl/tabs_agent_execute_test.go`
- 更新 `atlasx/cmd/atlasctl/tabs.go`
- 新增 `atlasx/internal/daemon/tab_agent_execute.go`
- 新增 `atlasx/internal/daemon/tab_agent_execute_test.go`
- 更新 `atlasx/internal/daemon/tab_agent_plan.go`
- 更新 `atlasx/internal/daemon/bootstrap.go`

## 实现事实

- `agentplan.Execute` 当前行为:
  - 缺少显式确认直接失败
  - 先按当前页重新生成 `agent-plan`
  - 仅执行 `sidebar_summarize` 与 `sidebar_ask`
  - `related_tab`、`memory_snippet` 等 preview-only step 明确拒绝
- 当前执行边界:
  - 复用既有 `tabs.Capture`
  - 复用既有 `memory.FindRelevantSnippets`
  - 复用既有 `sidebar.Config.AskWithMemory` 与 `SummarizeWithMemory`
  - 复用既有 `sidebar.SaveRuntimeResult`
- 明确不做的内容:
  - 不执行隐式浏览器跳转
  - 不执行多步链式自动运行
  - 不写 `memory/events.jsonl`

## 验证记录

- `cd atlasx && go test ./internal/agentplan`
  - 退出码: 0
- `cd atlasx && go test ./cmd/atlasctl -run 'TestTabsAgentExecute|TestTabsAgentPlan'`
  - 退出码: 0
- `cd atlasx && go test ./cmd/atlasctl -run 'TestTabsAgentExecute|TestTabsAgentPlan|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./internal/daemon -run 'TestTabAgentExecute|TestTabAgentPlan'`
  - 退出码: 0
- `cd atlasx && go test ./internal/daemon -run 'TestTabAgentExecute|TestTabAgentPlan|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- CLI 与 HTTP 共用同一套 agent execute 真相，没有分叉成第二套 provider 调用逻辑
- 成功执行后会写 sidebar runtime state，但不会写 memory events
- 当前交付的是可控的单步执行器，不是自动多步 agent
