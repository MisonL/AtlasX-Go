# CR-T040

- 任务: T040 暴露 runtime plan HTTP API
- 日期: 2026-04-01
- 结论: 通过

## 变更范围

- 新增 `GET /v1/runtime/plan`
- 新增 `POST /v1/runtime/plan`
- 新增 `POST /v1/runtime/plan/clear`

## 验证记录

1. `cd atlasx && go test ./internal/daemon ./internal/managedruntime`
   - 退出码: 0
   - 摘要: daemon 与 managedruntime 定向测试通过，覆盖 runtime plan 状态、创建、清理和错误 URL。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，无回归。

3. `cd atlasx && go build -o /tmp/atlasd-t040 ./cmd/atlasd`
   - 退出码: 0
   - 摘要: 构建临时守护进程二进制用于在线 HTTP 验证。

4. `HOME=$(mktemp -d /tmp/atlasx-t040-XXXXXX) /tmp/atlasd-t040 --listen 127.0.0.1:18541`
   - 退出码: 0
   - 摘要: 守护进程在空 runtime plan 状态下启动成功。

5. `curl -s http://127.0.0.1:18541/v1/runtime/plan`
   - 退出码: 0
   - 摘要: 返回 JSON 含 `present=false`，其余 plan 字段为空。

6. `curl -s -X POST http://127.0.0.1:18541/v1/runtime/plan -H 'Content-Type: application/json' -d '{"version":"123.0.0","channel":"stable","url":"https://example.com/chromium.zip","sha256":"deadbeef","archive_path":"/tmp/chromium.zip","bundle_path":"/tmp/Chromium.app"}'`
   - 退出码: 0
   - 摘要: 返回 JSON 含 `present=true`、`current_phase=planned`、`source_url=https://example.com/chromium.zip`。

7. `curl -s -X POST http://127.0.0.1:18541/v1/runtime/plan/clear`
   - 退出码: 0
   - 摘要: 返回 `cleared_install_plan=.../runtime/install-plan.json`。

## 审查结论

- runtime plan HTTP API 已形成服务化下载准备入口，但仍然只维护计划文件，不触发真实下载。
- 查询接口保持只读 `GET`，创建和清理动作使用 `POST`，边界清晰。
