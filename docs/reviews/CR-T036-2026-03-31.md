# CR-T036

- 任务: T036 暴露 runtime verify HTTP API
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- 新增 `POST /v1/runtime/verify`
- 复用 `managedruntime.Verify`
- 统一把已知校验失败映射为 `409`，保留结构化 JSON 响应

## 验证记录

1. `cd atlasx && go test ./internal/daemon ./internal/managedruntime`
   - 退出码: 0
   - 摘要: daemon 与 managedruntime 定向测试通过，覆盖 verify 路由、方法限制和失败状态码。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，无回归。

3. `cd atlasx && go build -o /tmp/atlasctl-t036 ./cmd/atlasctl && go build -o /tmp/atlasd-t036 ./cmd/atlasd`
   - 退出码: 0
   - 摘要: 构建临时二进制用于稳定的在线 HTTP 验证，避免 `go run` 冷启动噪声。

4. `HOME=$(mktemp -d /tmp/atlasx-t036-missing-XXXXXX) /tmp/atlasd-t036 --listen 127.0.0.1:18538`
   - 退出码: 0
   - 摘要: 守护进程在无 staged runtime 的临时 HOME 下启动成功。

5. `curl -s -X POST http://127.0.0.1:18538/v1/runtime/verify`
   - 退出码: 0
   - 摘要: 返回 JSON 含 `manifest_present=false`、`verified=false`、`error="managed runtime manifest is not present"`。

6. `HOME=$(mktemp -d /tmp/atlasx-t036-ok-XXXXXX) /tmp/atlasctl-t036 runtime stage --bundle-path "/Applications/Google Chrome.app" --version "local-google-chrome"`
   - 退出码: 0
   - 摘要: 在临时 HOME 下完成真实本机 `Google Chrome.app` 的 managed stage。

7. `HOME=<临时HOME> /tmp/atlasd-t036 --listen 127.0.0.1:18539`
   - 退出码: 0
   - 摘要: 守护进程在 staged runtime 环境下启动成功。

8. `curl -s -X POST http://127.0.0.1:18539/v1/runtime/verify`
   - 退出码: 0
   - 摘要: 返回 JSON 含 `manifest_sha256`、`actual_sha256`、`verified=true`，字段与 staged runtime 一致。

9. `rm -f /tmp/atlasctl-t036 /tmp/atlasd-t036 && chmod -R u+w <临时HOME> && rm -rf <临时HOME>`
   - 退出码: 0
   - 摘要: 临时二进制和临时 HOME 已清理，未污染默认工作环境。

## 审查结论

- `POST /v1/runtime/verify` 已形成独立门禁能力，不会把失败静默包装成成功。
- 返回体既保留 `verified` 等结构化字段，也保留 `error` 文本，可直接支撑后续控制面联调。
