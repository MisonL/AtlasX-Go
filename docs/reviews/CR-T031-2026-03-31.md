# CR-T031

- 任务: T031 改为 manifest 驱动的 managed binary 解析
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/internal/managedruntime/manifest.go`
- `atlasx/internal/managedruntime/bundle.go`
- `atlasx/internal/managedruntime/bundle_test.go`
- `atlasx/internal/managedruntime/status.go`
- `atlasx/internal/managedruntime/stage.go`
- `atlasx/internal/platform/chrome/runtime.go`
- `atlasx/internal/platform/chrome/chrome_test.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含 manifest binary path 与 managed runtime 检测测试。

2. `tmp=$(mktemp -d); src="$tmp/src/Google Chrome.app/Contents/MacOS"; mkdir -p "$src"; printf '#!/bin/sh\nexit 0\n' > "$src/Google Chrome"; chmod +x "$src/Google Chrome"; printf '<plist></plist>\n' > "$tmp/src/Google Chrome.app/Contents/Info.plist"; HOME="$tmp" go run ./cmd/atlasctl runtime stage --bundle-path "$tmp/src/Google Chrome.app" --version 123.0.0 >/dev/null; HOME="$tmp" go run ./cmd/atlasctl doctor`
   - 退出码: 0
   - 摘要: 输出 `chrome_source=managed_auto`，`chrome_binary` 指向 manifest 记录的 `.../Contents/MacOS/Google Chrome`。

3. `HOME="$tmp" go run ./cmd/atlasctl launch-webapp --dry-run`
   - 退出码: 0
   - 摘要: 输出 `binary=.../Contents/MacOS/Google Chrome` 与 `runtime_source=managed_auto`。

## 审查结论

- managed runtime 解析已优先使用 manifest 的 `binary_path`，不再绑定单一 `Chromium` 文件名。
- manifest 缺失时仍保留 legacy fallback，不会破坏已有 staged runtime 路径。
