# CR-T069

- 任务: T069 补齐 sidebar status 的 CLI 入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 新增 `atlasctl sidebar status`，输出 sidebar 配置状态、provider readiness、runtime 护栏和最近错误/trace
- 新增共享 `sidebar.Status.Render()` 文本渲染
- 补充 CLI 与 sidebar 渲染测试
- 更新 README 文档，收口新的 CLI 入口事实

## 验证记录

1. `cd atlasx && go test ./internal/sidebar`
   - 退出码: 0
   - 摘要: `Status.Render()` 输出 provider registry、runtime 护栏和最近错误字段通过。

2. `cd atlasx && go test ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: `atlasctl sidebar status` 的未配置与已配置 provider 输出通过。

3. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，新增 sidebar CLI 未破坏 runtime、tabs、daemon 与 memory 主链。

## 审查结论

- 本轮补齐了 sidebar 状态在 CLI 控制面上的缺口，本地操作者不再只能通过 HTTP 才能看到 provider readiness 和最近失败原因。
- CLI 与 HTTP 继续共用 `sidebar.StatusWithRuntime` 这一状态真相，没有引入第二套 readiness 判断逻辑。
- 输出仅包含 `api_key_env` 这类环境变量名，不会把真实 provider 密钥写入终端。
