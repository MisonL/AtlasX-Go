# CR-T116

- 日期: 2026-04-08
- 任务: `T116 补齐 Agent 预演步骤可执行性元数据`
- 结论: 已完成 `agent-plan` 步骤级可执行性元数据输出，CLI 与 HTTP 共用 `agentplan.Build` 单一规则推导 `executable`、`execution_path`、`requires_provider`

## 变更范围

- 更新 `atlasx/internal/agentplan/plan.go`
- 更新 `atlasx/internal/agentplan/plan_test.go`
- 更新 `atlasx/cmd/atlasctl/tabs_agent_plan.go`
- 更新 `atlasx/cmd/atlasctl/tabs_agent_plan_test.go`
- 更新 `atlasx/cmd/atlasctl/contract_test.go`
- 更新 `atlasx/internal/daemon/tab_agent_plan_test.go`
- 更新 `atlasx/internal/daemon/contract_test.go`

## 实现事实

- `Step` 新增字段：
  - `executable`
  - `execution_path`
  - `requires_provider`
- 统一推导规则 `deriveStepExecutionMeta(kind)`：
  - `sidebar_summarize` -> `executable=true` / `execution_path=sidebar_summarize` / `requires_provider=true`
  - `sidebar_ask` -> `executable=true` / `execution_path=sidebar_ask` / `requires_provider=true`
  - `related_tab` -> `executable=true` / `execution_path=tabs_activate` / `requires_provider=false`
  - `memory_snippet` -> `executable=true` / `execution_path=sidebar_memory_ask` / `requires_provider=true`
  - 其他 -> `executable=false` / `execution_path=preview_only` / `requires_provider=false`
- 未改变执行语义：
  - `read_only=true`、`executed=false` 与 `requires_confirmation=true` 口径保持不变

## 验证记录

- `cd atlasx && go test ./internal/agentplan`
  - 退出码: 0
- `cd atlasx && go test ./cmd/atlasctl -run 'TestTabsAgentPlan|TestTabsAgentExecute|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./internal/daemon -run 'TestTabAgentPlan|TestTabAgentExecute|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- CLI 与 HTTP 没有各自重复判断步骤可执行性，均复用 `agentplan.Build` 的统一规则
- 预演层新增观测不改变执行层行为，执行边界和失败口径保持稳定
