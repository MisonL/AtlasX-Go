# CR-T112

- 日期: 2026-04-08
- 任务: `T112 补齐 Agent 预演计划只读入口`
- 结论: 已完成 `atlasctl tabs agent-plan` 与 `GET /v1/tabs/agent-plan`，CLI 与 HTTP 共用同一套 `internal/agentplan.Plan` 主链，基于当前页上下文、页面建议、上下文推荐与 memory 检索生成结构化只读多步计划，不执行任何浏览器动作或 provider 调用

## 变更范围

- 新增 `atlasx/internal/agentplan/plan.go`
- 新增 `atlasx/internal/agentplan/plan_test.go`
- 新增 `atlasx/cmd/atlasctl/tabs_agent_plan.go`
- 新增 `atlasx/cmd/atlasctl/tabs_agent_plan_test.go`
- 更新 `atlasx/cmd/atlasctl/tabs.go`
- 更新 `atlasx/cmd/atlasctl/contract_test.go`
- 新增 `atlasx/internal/daemon/tab_agent_plan.go`
- 新增 `atlasx/internal/daemon/tab_agent_plan_test.go`
- 更新 `atlasx/internal/daemon/bootstrap.go`
- 更新 `atlasx/internal/daemon/contract_test.go`

## 实现事实

- `agentplan.Plan` 当前统一导出:
  - `goal`
  - `read_only`
  - `executed`
  - `returned`
  - `memory_returned`
  - `suggestion_returned`
  - `recommendation_returned`
  - `rollback`
  - `guardrails`
  - `steps`
- 计划生成主链:
  - 先复用既有 `tabs.Capture`
  - 再复用 `memory.FindRelevantSnippets`
  - 再复用 `suggestions.ForPage`
  - 再复用 `contextrec.ForPage`
  - 最后统一收敛为只读 `agentplan.Build(...)`
- 明确不做的内容:
  - 不执行标签切换、导航、打开页面、provider 请求或其他浏览器动作
  - 不引入第二套页面理解或检索逻辑
  - 不伪造 `executed=true`

## 验证记录

- `cd atlasx && go test ./internal/agentplan`
  - 退出码: 0
- `cd atlasx && go test ./cmd/atlasctl -run 'TestTabsAgentPlan|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go test ./internal/daemon -run 'TestTabAgentPlan|Test.*Contract'`
  - 退出码: 0
- `cd atlasx && go run ./cmd/atlasctl tabs agent-plan tab-1`
  - 退出码: 1
  - 关键输出:
    - `atlasctl: no managed browser session`
  - 解释: 当前开发机没有受管浏览器会话，因此真实命令按现有控制面门禁显式失败；这符合当前产品边界，不属于伪成功或静默降级
- `cd atlasx && go test ./...`
  - 退出码: 0

## 审查结论

- CLI 与 HTTP 共用同一套 Agent plan 聚合主链，没有分叉实现
- 新入口严格保持 `read_only=true` 与 `executed=false`，并显式给出 `requires_confirmation` 与 `rollback`
- 当前交付的是 `Agent 模式基础版` 的只读预演层，不是自动执行器
