# CR-T104

- 日期: 2026-04-08
- 任务: `T104 补齐按标签页查看指定 DevTools 面板 URL 入口`
- 结论: 已完成。`atlasctl tabs devtools-panel` 与 `GET /v1/tabs/devtools-panel` 已落地，CLI 与 HTTP 共用 `tabs.DevToolsPanel` 主链，先解析指定标签页的 `devtools_frontend_url`，再仅在 URL 边界显式追加 `panel` 参数并返回结构化只读 DevTools target。

## 变更事实

- 新增 `tabs.DevToolsPanel(...)`
- 新增 CLI:
  - `atlasctl tabs devtools-panel <target-id> <panel>`
- 新增 HTTP:
  - `GET /v1/tabs/devtools-panel?id=<target-id>&panel=<panel>`
- 新增测试:
  - `atlasx/internal/tabs/devtools_test.go`
  - `atlasx/cmd/atlasctl/tabs_devtools_panel_test.go`
  - `atlasx/internal/daemon/tab_devtools_panel_test.go`
  - `atlasx/internal/daemon/tab_devtools_panel_contract_test.go`

## 审查结论

- 已确认入口只复用现有两条主链:
  - 先走 `DevTools(targetID)` 解析 `devtools_frontend_url`
  - 再对解析结果仅追加显式 `panel` 参数
- 已确认 CLI 与 HTTP 共用同一套 devtools panel 只读主链:
  - 都调用 `tabs.DevToolsPanel(...)`
- 已确认失败显式暴露:
  - `panel` 缺失时显式返回错误
  - `panel` 非法时显式返回错误
  - `target` 不存在时显式返回错误
  - `devtools_frontend_url` 缺失时显式返回错误

## 验证记录

- 命令:
  - `cd atlasx && go test ./internal/tabs ./internal/daemon ./cmd/atlasctl`
  - `cd atlasx && go test ./...`
- 结果:
  - 均通过
