# CR-T050

- 任务: T050 建立 sidebar 多 provider registry 状态面
- 日期: 2026-04-06
- 结论: 通过

## 变更范围

- 为 `settings.Config` 追加 `sidebar_default_provider` 与 `sidebar_providers[]`
- provider registry 只记录 `api_key_env` 等环境变量名，不引入真实密钥字段
- `sidebar.FromSettings` 增加新旧配置桥接：存在 registry 时以 registry 为准，否则回退到旧单 provider 字段
- `/v1/sidebar/status` 与 `/v1/status` 增加 default provider / provider list 状态输出
- 补充 settings/sidebar/daemon 测试，覆盖 registry 读写、桥接优先级与 JSON 契约

## 验证记录

1. `cd atlasx && go test ./internal/settings`
   - 退出码: 0
   - 摘要: provider registry 保存、加载以及 `api_key_env` 落盘口径通过。

2. `cd atlasx && go test ./internal/sidebar`
   - 退出码: 0
   - 摘要: registry 状态输出、legacy 桥接与 registry 优先级测试通过。

3. `cd atlasx && go test ./internal/daemon`
   - 退出码: 0
   - 摘要: `/v1/sidebar/status` 与 `/v1/status` 的新增字段契约通过。

4. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，多 provider 状态面没有破坏现有控制面与 ask 错误口径。

## 审查结论

- sidebar registry 与 default provider 现在有统一状态真相，HTTP 状态面和 bootstrap 摘要都从同一份 `sidebar.Status()` 派生。
- 旧 `sidebar_provider/sidebar_model/sidebar_base_url` 仍可桥接为单 provider 配置；一旦新 registry 存在，就显式以 registry 为准。
- 配置文件只保存环境变量名，不保存真实密钥，满足当前阶段的安全边界。
