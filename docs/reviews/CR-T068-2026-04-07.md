# CR-T068

- 任务: T068 补齐 tabs 的 CLI selection 与 devtools 入口
- 日期: 2026-04-07
- 结论: 通过

## 变更范围

- 新增 `atlasctl tabs selection <target-id>`，输出原生选区文本与长度/截断元数据
- 新增 `atlasctl tabs devtools <target-id>`，输出当前标签页对应的 `devtools_frontend_url`
- 将 `tabs` CLI 实现从 `main.go` 拆分到 `cmd/atlasctl/tabs.go`
- 同步更新 README、阶段对齐与当前状态文档

## 验证记录

1. `cd atlasx && go test ./cmd/atlasctl`
   - 退出码: 0
   - 摘要: `tabs capture` 既有链路通过，新增 `tabs selection` 与 `tabs devtools` 的成功/失败输出测试通过。

2. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全仓回归通过，CLI 能力补齐未破坏 daemon、tabs、sidebar、runtime 主链。

## 审查结论

- 本轮补齐了 `tabs` 能力在 CLI 控制面上的缺口，避免 `/v1/tabs/selection` 和 `/v1/tabs/devtools` 只存在 HTTP 入口而没有对应命令行入口。
- CLI 与 tabs client 继续共用同一套 selection/devtools 主链，没有引入第二套解析逻辑。
- `tabs capture`、`tabs selection` 和 `tabs devtools` 已拆到独立文件，`main.go` 长度收回到约束内，降低后续继续扩展 tabs 命令时的复杂度。
