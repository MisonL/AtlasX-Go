# CR-T024

- 任务: T024 实现自管 runtime 安装清单骨架
- 日期: 2026-03-31
- 结论: 通过

## 变更范围

- `atlasx/internal/platform/macos/paths.go`
- `atlasx/internal/managedruntime/manifest.go`
- `atlasx/internal/managedruntime/manifest_test.go`
- `atlasx/internal/diagnostics/doctor.go`
- `atlasx/internal/daemon/bootstrap.go`
- `atlasx/README.md`
- `README.md`
- `tasks.csv`

## 验证记录

1. `cd atlasx && go test ./...`
   - 退出码: 0
   - 摘要: 全量 Go 测试通过，包含 manifest 读写与缺失状态测试。

2. `tmp=$(mktemp -d); runtime_root="$tmp/Library/Application Support/AtlasX/runtime"; mkdir -p "$runtime_root"; printf '{"version":"123.0.0","channel":"stable","sha256":"deadbeef","bundle_path":"/Applications/Chromium.app","installed_at":"2026-03-31T00:00:00Z"}\n' > "$runtime_root/manifest.json"; HOME="$tmp" go run ./cmd/atlasctl doctor`
   - 退出码: 0
   - 摘要: 输出 `managed_runtime_manifest_present=true` 以及 version/channel/bundle 口径。

3. `tmp=$(mktemp -d); runtime_root="$tmp/Library/Application Support/AtlasX/runtime"; mkdir -p "$runtime_root"; printf '{"version":"123.0.0","channel":"stable","sha256":"deadbeef","bundle_path":"/Applications/Chromium.app","installed_at":"2026-03-31T00:00:00Z"}\n' > "$runtime_root/manifest.json"; HOME="$tmp" go run ./cmd/atlasd --once`
   - 退出码: 0
   - 摘要: JSON 状态输出包含 `runtime_manifest_path`、`runtime_manifest_present`、`runtime_manifest_version`、`runtime_manifest_channel`、`runtime_manifest_bundle_path`。

## 审查结论

- manifest 只描述安装事实，不会被诊断口径误当成可执行 runtime。
- 状态面在无 manifest 文件时显式返回 `present=false`，不存在伪造安装成功路径。
- 该骨架为后续下载器或内置分发预留了最小事实源和状态导出口径。
